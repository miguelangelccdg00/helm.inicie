{
  "name": "api-auth-helm",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/api-auth-helm/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/api-auth-helm",
        "main": "apps/api-auth-helm/src/main.ts",
        "tsConfig": "apps/api-auth-helm/tsconfig.app.json",
        "assets": []
      }
    },
    "serve": {
      "executor": "@nx/node:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "api-auth-helm:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "api-auth-helm:build:development"
        },
        "production": {
          "buildTarget": "api-auth-helm:build:production"
        }
      }
    }
  },
  "tags": []
}